# RAG Q&A Bot — Python Dependencies
# Install with:  pip install -r requirements.txt

# ── Core RAG dependencies ──────────────────────────────────────────────────
sentence-transformers>=2.7.0   # local embedding model (all-MiniLM-L6-v2)
faiss-cpu>=1.8.0               # vector similarity search (CPU build)
groq>=0.9.0                     # Groq LLM API client (free tier)

# ── Document parsing ───────────────────────────────────────────────────────
pypdf>=4.0.0                   # PDF text extraction
python-docx>=1.1.0             # DOCX support (bonus)

# ── Web UI (bonus) ─────────────────────────────────────────────────────────
streamlit>=1.35.0              # optional web interface

# ── Utilities ──────────────────────────────────────────────────────────────
numpy>=1.26.0

# ── Dev / document generation only (not needed at runtime) ────────────────
# reportlab>=4.0.0              # only used to generate the sample PDF
